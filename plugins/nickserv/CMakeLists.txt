CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

SET(PLUGIN_NICKSERV ON CACHE BOOLEAN "nickserv plugin")

IF(PLUGIN_NICKSERV)
  TARGET_SOURCES(alice PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/nickserv.c")
  ADD_DEFINITIONS(-DPLUGIN_NICKSERV)
ENDIF()
